import { forEach, map, count, toArray, only } from './ReactChildren';
import {
  createElement,
  createFactory,
  cloneElement,
  isValidElement,
} from './ReactElement';

const Children = {
  map,
  forEach,
  count,
  toArray,
  only,
};

export {
  Children,
  createElement,
  createFactory,
  cloneElement,
  isValidElement,
};
