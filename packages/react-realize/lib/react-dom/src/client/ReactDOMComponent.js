const diffProperties = (
  domElement,
  tag,
  lastRawProps,
  nextRawProps,
  rootContainerElement
) => {};

const diffHydratedProperties = (
  domElement,
  tag,
  rawProps,
  parentNamespace,
  rootContainerElement
) => {};

export { diffProperties, diffHydratedProperties };
